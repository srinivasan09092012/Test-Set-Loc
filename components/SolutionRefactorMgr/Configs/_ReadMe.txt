Copy one of the preconfigured xml configurations to the root directory and rename it to "RefactorConfig.xml".

You can also use the _RefactorTemplate.xml file to create a new configuration file.  The template also has documentation about each node
as well as the parameters and accepted values for each node.